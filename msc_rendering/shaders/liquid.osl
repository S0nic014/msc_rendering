shader liquid ( color baseColor = 1,
                float fillLevel=0.5,
                float scale=1,
                output color Cout=0,
                output float Aout=1) 
{
    point PP;
    PP=transform("shader",P) * scale;

    Aout = step(PP[1] , fillLevel);
    Cout=baseColor;
}